steps:
# Build Docker image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/clouderun-test', '.']

# Push image to registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/clouderun-test']

# Deploy to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - run
  - deploy
  - clouderun-test
  - --image
  - gcr.io/$PROJECT_ID/clouderun-test
  - --region
  - europe-west1
  - --platform
  - managed
  - --allow-unauthenticated

images:
- gcr.io/$PROJECT_ID/clouderun-test

options:
  logging: CLOUD_LOGGING_ONLY